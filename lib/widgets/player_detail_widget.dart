import 'package:flutter/material.dart';

class PlayerDetailWidget extends StatelessWidget {
  const PlayerDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
